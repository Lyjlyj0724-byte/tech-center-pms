# 技术中心项目管理系统 — 需求框架与数据模型设计

> 版本：v0.1（草案，待技术中心确认工序数据后定稿）
> 日期：2026-09-07
> 关联系统：技术中心设备看板（tech-center-board）、工单分类器（ticket-classifier）

---

## 1. 背景与目标

技术中心需要一套内部项目管理系统，用于跟踪每个项目从立项到完工的全过程。项目的**工序清单由技术中心后续提供**，因此系统必须把工序做成**可配置、可批量导入的主数据**，而不是写死在代码里。

**核心目标：**

1. 工序模板库集中维护，技术中心提供的工序清单一键导入
2. 创建项目时套用工序模板，自动生成项目工序路线，允许单项目微调
3. 按工序采集进度，自动计算项目整体进度与延期预警
4. 为后续与设备看板、工单系统联动预留接口

---

## 2. 用户角色

| 角色 | 主要操作 |
|---|---|
| 系统管理员 | 导入/维护工序模板、用户与基础数据管理 |
| 项目负责人 | 创建项目、套用模板、调整本项目工序、填报工序进度 |
| 技术中心领导 | 查看全部项目进度看板、延期预警列表 |
| 普通成员 | 查看本人负责工序、更新工序状态 |

> 一期可用最简单的角色区分（管理员 / 普通用户两级），复杂权限待确认后再加（见第 9 节待澄清问题）。

---

## 3. 功能范围（一期做什么）

### 3.1 工序模板管理
- 工序模板的 Excel 批量导入（格式见第 7 节），导入前完整校验并给出错误明细
- 模板的增删改查、版本管理（模板被项目引用后不可物理删除，只能停用）
- 模板内工序：序号、编码、名称、标准工时、前置依赖、建议设备类型

### 3.2 项目管理
- 项目 CRUD：编号、名称、负责人、优先级、计划起止日期、状态
- 创建项目时选择工序模板 → 复制生成**项目自己的工序路线**（与模板解耦，模板后续修改不影响在建项目）
- 项目工序路线允许增删工序、调整顺序、修改计划时间

### 3.3 进度采集
- 每道工序状态流转：未开始 → 进行中 → 已完成（支持暂停）
- 记录实际开始/完工时间、进度百分比、备注
- 项目整体进度 = Σ(工序标准工时 × 完成比例) / Σ(标准工时)，按工时加权而非简单平均

### 3.4 进度看板与预警
- 项目列表：进度条、状态、计划 vs 实际
- 延期预警：当前日期 > 工序计划完工日期且未完成 → 标红；项目级汇总
- 单项目甘特视图（按工序计划时间渲染）

### 3.5 系统联动（预留，二期）
- 工序可关联设备看板中的设备 → 查看设备占用/负荷
- 工序异常可一键生成工单 → 进入工单分类器流转

---

## 4. 明确不做（一期排除项）

- 不做复杂的工作流引擎 / BPMN
- 不做成本核算、物料管理（BOM）
- 不做移动端 App（响应式 Web 即可）
- 不做多租户、对外接口开放平台
- 不自动排产（工序计划时间人工填写，系统只做校验与预警）

---

## 5. 验收标准

| # | 验收项 | 验证方式 |
|---|---|---|
| 1 | 技术中心提供的工序清单 Excel 能一次导入成功；含错误的文件能指出具体行号和原因 | 用真实清单 + 故意造错的文件各测一次 |
| 2 | 用模板创建项目后，项目工序与模板一致且可独立修改，改模板不影响已建项目 | 创建 → 改模板 → 核对项目工序未变 |
| 3 | 工序状态流转和进度填报后，项目整体进度按工时加权正确计算 | 构造已知数据核对百分比 |
| 4 | 过期待完工工序出现在预警列表并标红 | 把计划日期改到过去验证 |
| 5 | lint / test / build 三道关卡全绿，走 PR 合并 | CI 检查 |

---

## 6. 数据模型

技术方案对齐 tech-center-board：Node 后端 + JSON 文件存储（db.json），数据结构即以下集合。若后期数据量增大，可平迁到 SQLite，字段不变。

### 6.1 ER 关系

```mermaid
erDiagram
    process_template ||--o{ process_template_step : 包含
    project ||--o{ project_process : 实例化
    project }o--|| process_template : 套用于
    project_process ||--o{ progress_log : 记录
    project_process }o--o| equipment : 关联(预留)
    project_process ||--o{ ticket : 异常工单(预留)
```

### 6.2 工序模板 `process_templates`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | string | 是 | 系统生成 |
| code | string | 是 | 模板编号，唯一，如 `TMPL-MECH-01` |
| name | string | 是 | 模板名称，如「机械加工标准路线」 |
| category | string | 否 | 工序段分类，如 机加/装配/电气 |
| version | number | 是 | 版本号，导入时 +1 |
| status | enum | 是 | `active` / `disabled`（被项目引用后只能 disabled） |
| remark | string | 否 | 备注 |
| created_at / updated_at | datetime | 是 | 系统维护 |

### 6.3 模板工序 `process_template_steps`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | string | 是 | 系统生成 |
| template_id | string | 是 | 所属模板 |
| seq | number | 是 | 工序序号，模板内从 1 连续编号 |
| step_code | string | 是 | 工序编码，模板内唯一，如 `OP10` |
| step_name | string | 是 | 工序名称，如「粗加工」 |
| std_hours | number | 否 | 标准工时（小时），用于进度加权 |
| prev_codes | string[] | 否 | 前置工序编码列表（默认前一个工序；支持并行分支） |
| equipment_type | string | 否 | 建议设备类型，二期关联设备看板 |
| remark | string | 否 | 备注 |

### 6.4 项目 `projects`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | string | 是 | 系统生成 |
| code | string | 是 | 项目编号，唯一 |
| name | string | 是 | 项目名称 |
| owner | string | 是 | 项目负责人 |
| priority | enum | 是 | `高` / `中` / `低` |
| plan_start | date | 是 | 计划开始 |
| plan_end | date | 是 | 计划完工（≥ plan_start） |
| status | enum | 是 | `未启动` / `进行中` / `已完成` / `暂停` / `已取消` |
| template_id | string | 是 | 套用的工序模板 |
| remark | string | 否 | 备注 |
| created_at / updated_at | datetime | 是 | 系统维护 |

### 6.5 项目工序 `project_processes`（模板实例化产物）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | string | 是 | 系统生成 |
| project_id | string | 是 | 所属项目 |
| seq | number | 是 | 序号 |
| step_code / step_name | string | 是 | 从模板复制，可在项目内改 |
| std_hours | number | 否 | 从模板复制 |
| prev_ids | string[] | 否 | 前置项目工序 id（实例化后按 id 关联） |
| owner | string | 否 | 工序负责人 |
| plan_start / plan_end | date | 否 | 工序级计划时间 |
| actual_start / actual_end | datetime | 否 | 实际开始/完工 |
| status | enum | 是 | `未开始` / `进行中` / `已完成` / `暂停` |
| progress_pct | number | 是 | 0–100，完成时强制 100 |
| remark | string | 否 | 备注 |

### 6.6 进度记录 `progress_logs`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | string | 是 | 系统生成 |
| project_process_id | string | 是 | 对应项目工序 |
| record_date | date | 是 | 填报日期 |
| progress_pct | number | 是 | 当时进度 |
| note | string | 否 | 说明 |
| recorded_by | string | 是 | 填报人 |

### 6.7 预留字段

- `project_processes.equipment_id`：二期关联设备看板设备 id
- `tickets` 集合增加 `project_id` / `project_process_id`：二期对接工单分类器

---

## 7. 工序模板导入格式定义（关键交付）

**一句话给技术中心**：按模板 Excel 填两个 Sheet，系统导入时自动校验，有错会逐行提示。

文件：`templates/工序模板导入模板.xlsx`（已生成，可直接下发填写）

### Sheet 1「工序模板」— 必填

| 列 | 列名 | 必填 | 格式/校验规则 |
|---|---|---|---|
| A | 模板编号 | 是 | 同一模板的工序填相同编号；不同模板编号不同 |
| B | 模板名称 | 是 | 同一编号下名称须一致 |
| C | 工序序号 | 是 | 正整数，模板内从 1 开始连续，决定默认顺序 |
| D | 工序编码 | 是 | 模板内唯一，建议 OP10、OP20…（跳号便于以后插入） |
| E | 工序名称 | 是 | 不超过 50 字 |
| F | 标准工时(h) | 否 | 正数，用于进度加权；不填则该工序按等权处理 |
| G | 前置工序编码 | 否 | 多个用英文逗号分隔，如 `OP10,OP20`；必须是本模板内序号更小的编码；留空表示第一道工序 |
| H | 建议设备类型 | 否 | 如 数控车床/加工中心/三坐标 |
| I | 备注 | 否 | — |

### Sheet 2「项目清单」— 可选（有现成项目就一起导）

| 列 | 列名 | 必填 | 格式/校验规则 |
|---|---|---|---|
| A | 项目编号 | 是 | 唯一 |
| B | 项目名称 | 是 | — |
| C | 负责人 | 是 | — |
| D | 优先级 | 是 | 高 / 中 / 低 |
| E | 计划开始日期 | 是 | YYYY-MM-DD |
| F | 计划完工日期 | 是 | YYYY-MM-DD，≥ 开始日期 |
| G | 工序模板编号 | 是 | 必须存在于 Sheet 1 的模板编号 |

### 导入校验规则（系统侧实现）

1. 必填列缺失 → 报「第 N 行缺少 XX」
2. 工序序号不连续 / 编码重复 → 报具体行号
3. 前置工序编码不存在或序号 ≥ 当前行（循环依赖）→ 报错
4. 项目清单引用的模板编号在 Sheet 1 不存在 → 报错
5. 日期非法或完工早于开始 → 报错
6. **全部通过才写入**，任何一个错误则整体拒绝（避免半截数据）

---

## 8. 技术方案与任务拆分

**技术栈**（与 tech-center-board 完全一致，降低维护成本）：
React + Vite + TypeScript + Tailwind + shadcn/ui 前端；Node 后端 + db.json 存储；vitest 单测；eslint + husky 本地关卡；GitHub PR + CI 门禁。

**任务拆分**（每个切片一个 feature 分支 + 一个 PR）：

| 顺序 | feature 分支 | 内容 |
|---|---|---|
| 1 | `feature/init-scaffold` | 项目脚手架（从 tech-center-board 复制工程化配置）、CI、分支保护 |
| 2 | `feature/template-import` | 数据模型落地 + Excel 导入解析与全部校验规则 + 单测 |
| 3 | `feature/project-crud` | 项目 CRUD + 套用模板实例化工序路线 |
| 4 | `feature/progress-tracking` | 工序状态流转、进度填报、工时加权进度计算 |
| 5 | `feature/dashboard-alerts` | 项目进度看板、延期预警、单项目甘特视图 |

---

## 9. 待澄清问题（请技术中心确认）

1. **工序清单的实际形态**：目前是 Excel 还是纸质/PLM 系统导出？一个项目大概多少道工序？
2. **使用人数与并发**：同时在线大概多少人？决定 db.json 是否够用、要不要上 SQLite。
3. **登录与权限**：接不接域账号/统一认证？还是先单机免登录 + 简单口令？
4. **部署环境**：内网服务器一台？是否有 Node 运行环境限制（设备看板怎么部署的这个就怎么部署）？
5. **优先级与状态枚举**：高/中/低、项目状态五档是否符合现有习惯用语？
6. **是否需要审计**：谁改了工序时间要不要留痕（progress_logs 已留进度痕，改计划是否也要留）？
